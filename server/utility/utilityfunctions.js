import jwt from "jsonwebtoken";

const JwtCreatingFucntions = {
  creatActivationToken: async (newUser) => {},
  creatAccessToken: async (newUser) => {},
  creatRefreshToken: async (newUser) => {},
};
module.exports = JwtCreatingFucntions;
